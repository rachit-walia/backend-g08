/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Scan all JS, JSX, TS, TSX files in src folder
  ],
  theme: {
    extend: {
      // Add custom themes or extend default themes here
    },
  },
  plugins: [
    // Add Tailwind CSS plugins here if needed
  ],
}
